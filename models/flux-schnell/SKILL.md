---
name: flux-schnell
description: "Fast AI image generation with FLUX.1 Schnell via FairStack. Cheapest high-quality text-to-image. Black Forest Labs model. Triggers: flux, flux schnell, fast image, cheap image, black forest labs, flux 1"
---

# FLUX.1 Schnell

Budget drafts, rapid iteration, high-volume. Generates in ~1-2 seconds.

## Quick Start

```bash
fairstack image "your prompt" --model flux-schnell
```

## Pricing and Parameters

```bash
fairstack models --detail flux-schnell  # Current pricing, parameters, capabilities
```

## Related Skills

- `fairstack/skills@ai-image-generation` — All image models
- `fairstack/skills@cost-estimator` — Check cost before generating
- `fairstack/skills@compare-models` — Compare with other models
